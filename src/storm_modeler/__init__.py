"""storm_modeler — WxAlerts Storm Modeler Phase A.

A 3-pane desktop validation harness around the SCIT (``detection_v2``)
storm-cell identification & tracking package. Historical NWS warnings drive
archived radar volume pulls, volumes run through SCIT, and results render on a
self-rendered top-down map and a clickable nav tree, landing in PostGIS.
"""

__version__ = "0.1.0"
